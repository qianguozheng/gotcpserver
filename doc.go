package doc

/*
The minimal api

Communication: Client <=> Server
1. TCP socket
2. Commands: login, heartbeat,
			rcl， config_read(config),
			verification/notification(authentication)，
			reboot, upgrade, vpn(managment)


3. Optional: dns whitelist, resource/web such as config

*/
